# React Component Based Portfolio

Converted to React + Vite using only `src/components` for UI files. No `src/pages` folder is used.

## Run
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```

My new react portfolio
